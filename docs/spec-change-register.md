---
type: register
status: applied
tags: [register]
---

# OneGRC — Spec v2.0 → v2.1 Change Register

**Audit purpose:** find everything in the functional spec that is appropriate for a demo/prototype but must not flow into the production build, and correct both documents. This register lists every change. Severity: **Critical** = would cause a wrong build or rework of a foundation; **High** = would cause rework of a feature or a dishonest number in front of a customer; **Medium** = would cause confusion or drift, not rework.

**Documents changed:** `onegrc-functional-product-spec.md` (v2.0 → v2.1) · `onegrc-build-plan.md` (v1.0 → v1.1).

---

## Critical

| # | What changed | Where | Why | Build impact |
|---|---|---|---|---|
| C-01 | **Exception rebuilt as a first-class entity** (subject = control or obligation; `issueId` optional; proactive exception with no surfaced issue is a first-class path). The "design decision worth preserving" (exception = issue with a deviation record) is overridden and marked superseded. Remediation register becomes a **union view over Issues + Exceptions**, preserving `BR-LNK-06`'s intent. On expiry the exception enters a **review** (close / renew-extend with SoD / convert to accepted risk / lapse); **no auto-created issue on expiry**; an expired exception is itself the visible open exposure and escalates. New rules `BR-LFC-13`, `BR-AUT-11` (escalating renewal authority: 2nd renewal needs Executive; beyond 2nd reported by name to the Audit Committee). | §5.14, §5.22, §7.1, §7.2, §7.3, §6.7, §6.8 (`BR-LNK-06` wording), §22 glossary | Fusing two lifecycles (remediation and governed deviation) into one object was a prototype shortcut; a proactive deviation is not a weakness and must not be forced to masquerade as one | Yes — schema (Exception table, not ExceptionRecord-on-Issue), union-view register, new review workflow. Build plan §3.2 rewritten, P2-04 amended, new chunk P2-13 |
| C-02 | **Identifier scheme replaced** per the locked decision: catalogue records `TYPE-NNNNN` (no year), event records `TYPE-YY-NNNN`, max 11 chars, no semantic middle segments; 2-char prefixes promoted (`WB`→`WBR`, `AP`→`APE`, `DA`→`DAS`, `WP`→`WPR`, `DSAR`→`DSR`); a `citation` field carries the legal reference; every record carries `title` + required `shortTitle` (≤60) with lists rendering shortTitle only. §7.4 rewritten; every identifier example in §7.1/§7.4 replaced. | §7.4, §7.1, §8 preamble | "Meaningful middle segments" (`SRC-PFRDA-INV-COMMITTEE`, 23 chars) were demo readability promoted into a convention; they broke layouts and encode meaning that belongs in fields | Yes — `core/ids` scheme, seed-transform id migration, `<EntityRef>` component, shortTitle column on every model. Build plan I-6 amended, new chunks P0-13, P1-18 |
| C-03 | **Obligation split into `Obligation` (standing duty, carries frequency) + `ObligationCycle` (instance)** made explicit in the object catalogue; cycle id = `<dutyId>.<period>`, never rendered inline. §7.1 previously gave one object with cycle states, contradicting §5.5's instances. | §7.1, §7.3, §7.4 | The single-object model was the prototype's patch-one-row shortcut; Requirement 11 and the on-time ledger need instance rows | Yes — already the build plan's §3.3 position; spec now agrees. Cycle-id convention pinned |
| C-04 | **`Overdue` declared derived, never stored** — new rule `BR-DRV-17`. §7.3 showed `Due → Overdue` as a stored obligation state while the platform's first principle is derive-don't-store. Applies to every dated item (tasks, campaign tasks, cycles). | §7.3, §6.6, §5.4 | A stored Overdue is exactly the "stored status drifts from the facts" failure §2 warns about; the prototype stored it for render convenience | Yes — schema has no Overdue state; serializers derive it (already build plan §3.4; spec now normative) |
| C-05 | **Task generalized to one work-item engine** with `completionPolicy` (`simple \| acknowledge \| evidence \| maker-checker`) gating legal transitions; stored states `Open · InProgress · Submitted · Returned · Done · Cancelled`; Overdue derived; display labels vary by policy. Used across obligations, remediation, campaigns, DSAR stages, attestations. Resolves the v2.0 overlap between task states and the maker-checker pattern (the pattern's states become projections). | §5.4, §5.7 note, §7.1, §7.3, §22 glossary | v2.0 carried two overlapping state vocabularies (task `Open·Evidence attached·Submitted·Verified` vs maker-checker `Drafted·Submitted·Approved·Returned`) — unimplementable as written without a private reconciliation per module | Yes — one Task table + policy column; campaign tasks and DSAR stages ride it. Build plan §3.2/§8(c) amended |
| C-06 | **§10.1 dishonest metric definitions corrected / superseded.** (a) "Enterprise risk level" (the hardcoded 7.8/10 with a string-literal "+0.3 QoQ") replaced by the derived tail-weighted aggregate (dashboard M10, `BR-DRV-05/07`). (b) "Finding age (mean)" and "Average remediation days (mean age of open issues)" **removed** — both improve when you close young issues or stall; replaced by ageing bands + oldest + %-within-SLA (M9/M19/M20). (c) On-time rate denominator fixed: was "over cycles **filed**" (not filing flattered it); now "over cycles that **fell due**" — unfiled counts against. (d) "Control coverage" split into **Control pass rate** and **Duty coverage**; bare "coverage" banned from the UI. §10.1 now defers board-surface selection, thresholds and layout to the dashboard design document rather than duplicating it. | §10.1, §10.3 cross-refs, §2, §5.8/5.9 side-effect wording | Headline numbers that are constants, means that reward doing less work, and a rate a firm can game by not filing are demo staging and vanity-metric defects on a platform whose pitch is honest numbers | Yes — metrics module built to the dashboard doc catalogue; P1-14/P2-11 amended |
| C-07 | **Fabricated trends outlawed normatively** — new rule `BR-DRV-18`: a trend point is the metric's own definition evaluated at a past instant from dated records; the final point equals the live value; no series or delta is ever stored or synthesized. §10.2 rewritten to defer windows/grains to the dashboard doc and to name the prototype's `Rand`-generated series as demo-only. | §10.2, §6.6 | `src/lib/trends.ts` fabricates all three cockpit series from a seeded RNG and the chart headlines are string literals — directly violating what §10.2 already required | Yes — trend endpoints compute from history; seed synthesizes historical *records* (not curves) so day-one trends derive honestly (G-26) |
| C-08 | **The demo "now" anchor and its full dependency set named, and confined to demo seeding.** Appendix A corrected (the prototype has in fact moved to an evergreen load-time anchor; every seeded date is an offset from it) and every dependency listed: offset-seeded deadlines, the marquee first-paint clock reading, synthesized history behind trends and ledgers, period-over-period deltas, ladder rungs derived-but-never-fired, "as at" metrics. Production answers stated per dependency (ClockService, real scheduler, real history, as-at stamping, sitting-over-sitting deltas). | Appendix A, §23 (new), G-05/G-26 wording | Everything downstream of the anchor silently assumes a world staged around one instant; unnamed, those assumptions leak into the build one at a time | Yes — build plan §4 clock-handling section amended (anchor already evergreen; parameterization stands) |

## High

| # | What changed | Where | Why | Build impact |
|---|---|---|---|---|
| H-01 | **Appendix A relabelled "the seeded demonstration world — demo only, not product requirements"** with an explicit boundary note: volumes are seed counts (§17.1 governs scale), headline figures are staged, and the "demo data principles" (no empty tables, non-round numbers, real-clock timestamps) are **seeding aesthetics only**, each given its production counterpart. | Appendix A | Demo realism rules were phrased as principles that "should carry into any future seeding" and were one step from being read as product constraints | Indirect — stops false requirements entering the backlog |
| H-02 | **Designed empty states made a requirement** (new §17.4 bullet): a register with no rows states why (no data yet, filtered to nothing, module not deployed, nothing to report — different messages); a metric without inputs renders "no data — reason"; empty is never faked and never zero. Round numbers declared legitimate output. | §17.4 | Production systems have empty tables and 100%/0 values on day one and after filters; the demo's "no empty tables anywhere" would otherwise be inherited as a UI rule | Yes — empty-state component work in rewiring chunks |
| H-03 | **§5.29 agent determinism requirement corrected**: "the same inputs produce the same result" (a property of the scripted prototype) replaced with the auditable requirement — every run records its inputs, evidence and outputs so proposals are reproducible and reviewable; bit-identical determinism is explicitly not required of a real model behind the seam. | §5.29 | A real model cannot honour bit-determinism; keeping it would make the requirement untestable or force the seam to stay scripted forever | Yes — agent_run persistence contract (already in plan §1.5); requirement wording now matches |
| H-04 | **Risk acceptance lapse state renamed `Acceptance lapsed`** (was `Exception expired`) to remove the name collision with the now-first-class Exception entity; a lapsed acceptance is the risk's own open, escalating exposure — it does **not** create an Exception record. Requirement 18 wording aligned. | §5.13, §7.3, §20.2 Req 18 | Two different lifecycles were sharing one state name; after C-01 the collision would read as "lapse auto-creates an exception", which is exactly the auto-spawn the locked decision forbids | Yes — risk state enum naming |
| H-05 | **Person↔Role made explicitly many-to-many** (§4.2 note: the roster's Role column is the primary role; several roles per person is the model — the two committee chairs already prove it) and the **§4.10 authority matrix declared data, not code**, maintained through governed configuration within §14.2's floor (SoD can never be disabled). New §14.1 row "Authority matrix". | §4.2, §4.10, §14.1 | The single-role roster column read as a 1:1 model; authority hard-coded per screen is `BR-AUT-01`'s named failure | Yes — PersonRole join (already planned); matrix as seed-loaded data with governed edits |
| H-06 | **"Coverage" vocabulary swept**: §2 and §5.8/5.9 side-effects now say "control pass rate"; §4.6 persona wants say "duty coverage"; §10.1 records the ban ("coverage" appears in no UI label except the compound "Duty coverage"); "attestation coverage" renamed "attestation rate" at its definition with the metric id kept. §21.11 and G-19 marked **decided**. | §2, §4.6, §5.8, §5.9, §10.1, §17.2, §19.2 G-19, §21.11 | One word carrying two meanings ("of controls tested, how many pass" vs "of duties owed, how many have a control") is the defect §10 itself warned about; the decision is made — the spec must stop presenting it as open | Yes — labels and metric functions |
| H-07 | **§23 added: "Demo-only constructs and their production answers"** — a table of every demo artifact (anchor, staged metrics, fabricated trends, synthesized history, persona-switcher auth, client-side authority, session audit log, seeded connector statuses, mocked filings/uploads, scripted intelligence, demo data aesthetics, demo volumes, semantic identifiers, marquee staging, sector-hardcoded routes), what each is, why it exists, and what production must do instead. | New §23 | The section that stops the team re-inheriting demo thinking; previously this knowledge was scattered or implicit | Indirect — review checklist for every phase |
| H-08 | **Persona switcher's target-state role clarified on the surfaces list**: §8.3 now describes it as a **view selector** over the roles/altitudes a person actually holds (impersonation is a dev-mode capability only, logged); v2.0 described the prototype behaviour as a product surface. | §8.3 | The switcher-as-auth is the prototype's single biggest simplification (G-02); the surfaces catalogue was quietly re-encoding it | Yes — consistent with plan §1.7 |
| H-09 | **Sector-specific hardcoded routes flagged**: Appendix B relabelled as the prototype's as-built route map (demo reference), with the note that production routing must be sector-neutral (`/pfrda`, `/dpdp` become configured sector/module slugs per §14.3). | Appendix B | A second sector pack (§14.3, §21.20) cannot ship on a URL scheme with one regulator's name baked in | Yes — routing naming in rewiring chunks |
| H-10 | **Escalation target naming fixed**: the ladder's "Compliance Officer" rung (a role that does not exist in §4.4) is defined as the configured compliance escalation owner — the Head of Compliance by default, resolved via the department-head map like every other rung. | §5.27, §6.5 `BR-ESC-01`, §11.3 | A builder cannot resolve an escalation target to a role that is not in the role model | Minor — config default |
| H-11 | **§0 / How-to-read / Appendix D updated to v2.1**; companion documents now name the dashboard KPI design doc (governing board surfaces and metric catalogue) and the build plan; "What changed in v2.1" added; Appendix C rule index updated for the new rule numbers. All existing `BR-*`, `G-*` and workflow numbers preserved; new rules take new numbers only. | §0, Appendix C, Appendix D | Document control; the build plan references rule ids and workflow numbers — none were renumbered | None |

## Medium

| # | What changed | Where | Why | Build impact |
|---|---|---|---|---|
| M-01 | §5.22 trigger list and §9 capability text adjusted: exceptions **appear in** the one remediation register (union view) rather than being "raised into" it as issues; attestation cannot-comply declarations create Exceptions (subject = the policy's control/obligation). | §5.22, §5.17, §9 | Follow-through of C-01 | Covered by C-01 |
| M-02 | Glossary entries for **Exception** and **Task** rewritten to the v2.1 models; **shortTitle/title** convention noted under identifiers. | §22, §7.4 | Glossary must not re-teach the superseded models | None |
| M-03 | §8.2 Integrations: demo-script phrase "The closing shot." removed; purpose statement kept. | §8.2 | Demo-narrative language in a functional requirement | None |
| M-04 | §4.2 roster introduced as **seed data of the demonstration world**; the requirement is the mechanism (named people, department heads, escalation resolution), not the 23 names. | §4.2 | Stops the roster reading as a product constant | None |
| M-05 | §21 open decisions: #11 (coverage) marked **Decided** with the resolution; a note added that the exception model and identifier scheme decisions were taken in v2.1 (recorded in §23/§5.14/§7.4); #15/#17 carry the build plan's defaults as notes. | §21 | Decisions already taken must not be re-litigated by a reader of §21 | None |
| M-06 | G-05/G-26 wording updated ("frozen now" → the demo anchor as described in the corrected Appendix A); G-19 marked decided. | §19.2 | Gap register accuracy | None |
| M-07 | Build plan updated to v1.1: governing spec v2.1; §3.2/§3.3/§3.4 exception + task models; I-6 identifier scheme; §4 anchor description; §8 defaults (b)/(c) resolved by decisions; P2-04/P1-14/P2-11/P3-01 amended; new chunks P0-13 (id scheme in the spike), P1-18 (`<EntityRef>` + shortTitle sweep), P2-13 (exception review workflow), P2-14 (honest cockpit: kill fabricated trends + enterprise-risk constant, dashboard-doc metrics), P3-10 (repeat-findings predecessor link, M21). | Build plan | The corrections create real work; the plan must carry it with stable chunk ids | Yes — the plan itself |

---

## Items needing the user's decision (not corrected, only flagged)

1. **Audit Committee Chair holds `Close a case` (fraud) and `Close a report` (speak-up) in §4.10** while §4.4/§4.7 say committee chairs review and never operate. Defensible as the escape hatch for cases implicating executives, but it is a real tension in the authority model — confirm it is intended, and consider confining it to cases where the executive set is recused.
2. **Obligation ↔ Control cardinality**: §7.2 fixes one satisfying control per obligation (`Control → obligation 1:many`). Real estates sometimes satisfy one duty with several controls. Duty coverage (M8) works either way; widening later is a migration. Confirm 1:many is intended.
3. **Retention floors** (§21.18) remain placeholders (10y/8y/8y) pending counsel — unchanged, but they gate P1-06.
4. **Committee-chair speak-up closure SoD**: with the ethics office being a person list, the SoD pairs for `Close a report`/`Unseal` should be checked against recusal in the worked org (small list, chair may be conflicted). §4.12 covers recusal; no text change made.

---

## Related in this vault

The decisions behind the largest changes: C-01 → [[ADR-005-exception-first-class]] · C-02 → [[ADR-003-identifier-scheme]] · C-03 → [[ADR-004-obligation-and-cycle]] · C-04/C-07 → [[ADR-008-metric-honesty]] (BR-DRV-17/18) · C-05 → [[ADR-006-task-work-item]] · C-06 → [[ADR-008-metric-honesty]] · H-05 → [[ADR-007-roles-and-authority]]. Rule groups the changes touched: [[BR-LFC]], [[BR-AUT]], [[BR-DRV]], [[BR-LNK]].
